version: "1.0"
services:
  postgres:
    image: postgres:14-alpine
    container_name: shortcut_example
    environment:
      POSTGRES_DB: "shortcut_example"
      POSTGRES_USER: "adm"
      POSTGRES_PASSWORD: "adm"
    volumes:
      - .:/docker-entrypoint-initdb.d
    ports:
      - "5432:5432"